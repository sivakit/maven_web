
pipeline {
    agent any
    properties {
    pipelineTriggers([cron('H/30 * * * *')])
    }
    stages {
        stage('build') {
            steps {
                sh 'echo'
            }
        }
    }
}
